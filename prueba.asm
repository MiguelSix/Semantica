;Archivo: prueba.asm
;Fecha: 07/11/2022 19:43:54
#make_COM#
include 'emu8086.inc'
ORG 100h
;Variables:
	area DW ?
	radio DW ?
	pi DW ?
	resultado DW ?
	x DW ?
	a DW ?
	d DW ?
	altura DW ?
	i DW ?
	j DW ?
	y DW ?
	z DW ?
MOV AX, 0
PUSH AX
POP AX
MOV j, AX
InicioFor1:
MOV AX, j
PUSH AX
MOV AX, 10
PUSH AX
POP BX
POP AX
CMP AX, BX
JGE FinFor1
MOV AX, 2
PUSH AX
ADD j, 2
MOV AX, j
PUSH AX
POP AX
CALL PRINT_NUM
PRINTN ""
JMP InicioFor1
FinFor1:
PRINTN ""
PRINTN ""
PRINTN ""
PRINTN ""
MOV AX, 2
PUSH AX
POP AX
MOV a, AX
MOV AX, 3
PUSH AX
POP AX
MOV d, AX
PRINTN ""
PRINTN ""
MOV AX, a
PUSH AX
POP AX
CALL PRINT_NUM
PRINTN ""
MOV AX, d
PUSH AX
POP AX
CALL PRINT_NUM
PRINTN ""
MOV AX, a
PUSH AX
MOV AX, d
PUSH AX
POP BX
POP AX
CMP AX, BX
JLE if1
JMP else1
if1:
MOV AX, 2
PUSH AX
POP AX
MOV d, AX
PRINTN "d es mayor que a"
MOV AX, a
PUSH AX
MOV AX, d
PUSH AX
POP BX
POP AX
CMP AX, BX
JNE if2
PRINTN "a es igual a d"
JMP else2
if2:
else2:
else1:
RET
DEFINE_SCAN_NUM
DEFINE_PRINT_NUM
DEFINE_PRINT_NUM_UNS
END
